<%= turbo_frame_tag "cart" do %>
  <h1>Cart</h1>
  <% if @cart.cartItems.count > 0 %>
    <%= render "cart/cartItems" %>
    <strong>Total</strong>
    <p><%=number_to_currency( @cart.total, unit: "$", precision: 2) %></p>
    <% else %>
    <p>Your cart is empty </p>
  <% end %>
<% end %>
